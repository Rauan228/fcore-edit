# VPS API base the browser calls. MUST be https on production (Vercel is https,
# an http API triggers a mixed-content block). Set this in Vercel → Project →
# Settings → Environment Variables as NEXT_PUBLIC_API_URL.
NEXT_PUBLIC_API_URL=https://your-vps-api.example.com
