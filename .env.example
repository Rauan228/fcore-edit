# VPS backend origin. Server-side only (NO NEXT_PUBLIC_ prefix) — the Next.js
# API routes proxy to it, so the browser never sees this URL and there is no
# mixed-content issue. Can stay http.
# Set in Vercel → Project → Settings → Environment Variables as API_ORIGIN.
API_ORIGIN=http://178.88.115.213:9101
